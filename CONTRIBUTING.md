# 贡献指南

## ⚠️ 第一条：不要破坏与上游的插件边界

本 skill **不自带渲染能力**，它依赖上游 [`guizang-ppt-skill`](https://github.com/op7418/guizang-ppt-skill)（作者 歸藏 [@op7418](https://x.com/op7418)，**AGPL-3.0**）已安装在本机。

**本仓库不包含上游的任何代码、模板或文档资产。** 运行时按路径读取上游目录。

所以以下改动**一律不接受**：

- 把上游的模板、CSS、动效、Sxx 版式、校验脚本复制进本仓库
- 把两个项目打成单一分发物（安装包、上架包、vendored 目录）
- 以"自包含""开箱即用""离线可用"为理由绕过上游依赖

原因不是洁癖，是**许可证**：本仓库对上游是"运行时依赖"而非"再分发"，两者是插件关系而不是合并关系。**这个前提一旦被打破，整个仓库就必须转为 AGPL-3.0 并保留署名。** 这条边界是本层能选 MIT 的唯一理由。

如果你确实需要一个合并版本，正确做法是**另开一个仓库并整体按 AGPL-3.0 授权**，而不是在这里放松边界。

## 第二条：`taste.md` 是这个 skill 的心脏

新反馈的处理顺序固定：

1. **先追加到 `references/taste.md`** —— 保留原话，标上日期和第几轮反馈
2. 再判断：要不要升格成 `SKILL.md` 的 P0 铁律？
3. 能机器判定的，同步写进 `scripts/validate-ppt.mjs`

追加格式：

```markdown
### N. 短标题
> "原话引用"（YYYY-MM-DD，第几轮反馈）
**判定**：什么情况算触发
**处理**：怎么改
```

**只改 SKILL.md 而不记 taste.md 的 PR 会被要求补上。** 原话是这份档案的价值所在——它记录了"为什么"，而"为什么"决定了边界怎么判。

## 第三条：改动必须能通过自检

改了校验器或影响了页面结构时：

```bash
node scripts/validate-ppt.mjs <一个真实的 deck.html>
```

校验器的新规则必须**能暴露真实失败**：附上一个会触发它的样例（正例 + 反例），不要只加一条永远不会命中的判定。

改了 `reviewer-persona.md` 的 prompt 模板时，说明它**实际跑过一次没有**。没跑过就写"未实测"。

## 不要提交

- 上游的任何资产（见第一条）
- 真实人名：`scripts/forbidden-names.txt` 保持模板状态，个人黑名单用 `--names=` 临时传
- 具体商业项目的材料、deck 产物、客户信息
- 未坐实的具体数字（位数、成交额、百分比）——这类内容应写成定性表述

## Commit 信息

```
口味：追加 XXX 反馈（附日期与轮次）
规则：XXX 升格为 P0 铁律
校验：新增 XXX 判定（附正反例）
文档：修正 XXX
```

## 提交 PR

描述里写清楚：**改了什么、依据什么反馈或问题、验证了什么、没验证什么。**

涉及 `taste.md` 的改动要说明反馈来源；涉及规则的改动要说明它和现有 10 条 P0 铁律是否冲突。
